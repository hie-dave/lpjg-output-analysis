% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site-budget.r
\name{render_site_budget}
\alias{render_site_budget}
\title{Render an OzFlux site budget}
\usage{
render_site_budget(
  repo,
  site,
  pft,
  output_file = paste0(site, "-budget.html"),
  source_id = "dave",
  source_label = source_id,
  description = "",
  data_dir = NULL,
  params = list(),
  quiet = FALSE
)
}
\arguments{
\item{repo}{Path to the simulation repository root, containing
\code{benchmarks/ozflux/outputs.ins} and the site directories.}

\item{site}{One site-directory name, for example \code{"CumberlandPlain"}.}

\item{pft}{PFT column to use for PFT-specific diagnostics, for example \code{"MRS"}.
This is not a report-wide filter for multi-PFT simulations.}

\item{output_file}{HTML destination, relative to the current working directory
or absolute. Parent directories are created if necessary. An existing file
is overwritten. Defaults to \verb{<site>-budget.html}.}

\item{source_id}{Model source identifier.}

\item{source_label}{Model source label used in plots.}

\item{description}{Text describing the model revision, configuration and run.}

\item{data_dir}{Optional supplementary-observation directory containing an
\code{awra} subdirectory. Defaults to NULL, using the registered observations
(including the bundled observations) without adding AWRA readers.}

\item{params}{Named list of presentation overrides: \code{aspect_ratio},
\code{image_fmt}, \code{fig_width}, \code{dpi}, \code{retina}, \code{fig_align}, and \code{out_width}.
Supply simulation settings through the named arguments above.}

\item{quiet}{Whether to suppress rmarkdown rendering progress.}
}
\value{
The absolute path to the rendered HTML file, invisibly.
}
\description{
Render the report shipped with daveanalysis without copying or editing its
template. Run the model first and retain the OzFlux benchmark directory
structure. See \code{vignette("site-budget", package = "daveanalysis")} for the
required outputs, columns and observation coverage.
}
\details{
The wrapper checks the directory layout and reports all missing
output files together. It accepts \code{.out} and \code{.out.gz} files. These checks
do not validate file contents, PFT columns or observation coverage.
Standard observations, including EucFACE, are bundled with the package.
Some report sections require particular observed series to be available
for the selected site. The report is rendered from a temporary directory;
no files are written into the installed package or simulation repository.
Rendering requires the suggested package rmarkdown and Pandoc.
}
\examples{
\dontrun{
render_site_budget(
  repo = "/path/to/lpj-guess", site = "CumberlandPlain", pft = "MRS",
  output_file = "reports/CumberlandPlain.html",
  description = "Model revision and simulation settings."
)
}
}
